{
  "_description": "Canonical v0.8 root component for the ConciergeForm surface. Field labels are BoundString objects per spec convention; `maxLength` is the camelCased equivalent of the standard TextField's validation hint.",
  "catalogId": "lumen.com:concierge/v1",
  "root": {
    "id": "c-root",
    "component": {
      "ConciergeForm": {
        "fields": [
          {"type": "toggle",  "name": "gift_wrap", "label": {"literalString": "Gift wrap (+$8)"}},
          {"type": "text",    "name": "note",      "label": {"literalString": "Gift note"}, "maxLength": 120},
          {"type": "address", "name": "ship_to",   "label": {"literalString": "Ship to"}}
        ],
        "action": {"name": "form"}
      }
    }
  }
}
